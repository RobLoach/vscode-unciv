{
  "$id": "https://raw.githubusercontent.com/RobLoach/vscode-unciv/refs/heads/master/schemas/TileResourcesAmount.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Tile Resource Amount",
  "type": "object",
  "description": "Specifies the amount of resources on a tile, categorized as sparse, default, or abundant.",
  "properties": {
    "sparse": { "type": "number" },
    "default": { "type": "number" },
    "abundant": { "type": "number" }
  },
  "required": ["sparse", "default", "abundant"]
}
